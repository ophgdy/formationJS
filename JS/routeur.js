/**
 * variable de config des routes
 */
const routeConfig={
    route:[]
}
/**
 * fonction de maintient de route
 */
export const handleRoute=() => {
    const pathName=location.pathname;
    console.log(pathName)
}
